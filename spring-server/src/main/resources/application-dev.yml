spring:
  datasource:
    url: jdbc:mysql://localhost:3306/securekey_locksmith_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  sql:
    init:
      mode: never

server:
  port: 8080
  servlet:
    context-path: /api

app:
  frontend-url: http://localhost:8081
  skip-verification: true

google:
  maps:
    api-key: "AIzaSyCJRnC2wZxTw_epmSEQB-Rb37zl_e6NovU"

management:
  health:
    mail:
      enabled: false
